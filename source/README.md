**LLMStats**

A dashboard widget to monitor locally hosted LLM servers directly on the Unraid dashboard. Supports Ollama and llama-server, shows available and loaded models with supported stats and busy/idle state, and provides optional model load/unload controls where the server mode supports them. llama-server single-model mode is monitor-only; router mode enables full model management.
