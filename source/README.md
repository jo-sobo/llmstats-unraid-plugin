**LLMStats**

A dashboard widget to monitor locally hosted LLM servers directly on the Unraid dashboard. Supports Ollama and llama-server (router mode), shows available and loaded models with supported stats and busy/idle state, and provides model load/unload controls.
